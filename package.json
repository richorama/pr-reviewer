{
  "name": "pr-reviewer",
  "version": "1.0.0",
  "description": "AI-powered PR reviewer for Azure DevOps and GitHub using GitHub Copilot SDK",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run build:azdo-task",
    "build:azdo-task": "mkdir -p azure-devops/task && cp dist/azure-pipelines-task.js azure-devops/task/ && cp dist/index.js azure-devops/task/ && cp -r dist/azure-devops azure-devops/task/ && cp -r dist/github azure-devops/task/ && cp -r dist/review azure-devops/task/ && cp -r dist/types azure-devops/task/ && cp package.json azure-devops/task/ && cd azure-devops/task && npm install --production",
    "package:azdo": "npm run build && cd azure-devops && tfx extension create --manifest-globs vss-extension.json",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "azure-devops",
    "pr-review",
    "copilot",
    "ai",
    "code-review"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@octokit/rest": "^20.0.2",
    "azure-devops-node-api": "^12.5.0",
    "azure-pipelines-task-lib": "^4.17.3",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.14",
    "jest": "^29.7.0",
    "tfx-cli": "^0.17.0",
    "ts-jest": "^29.2.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
